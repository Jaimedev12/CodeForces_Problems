#include <bits/stdc++.h>
using namespace std;

#define _ ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

int main() { _

    int n; cin >> n;
    string s; cin >> s;

    sort(s.begin(), s.end());

    cout << s << endl;

    return 0;

}